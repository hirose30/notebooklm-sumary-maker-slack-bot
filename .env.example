# NotebookLM Slack Bot - Environment Configuration
# Copy this file to .env and fill in your values

# ===================================
# Slack Configuration
# ===================================

# Slack Signing Secret (from Slack App Basic Information)
SLACK_SIGNING_SECRET=1234567890abcdefghijklmnopqrst

# ===================================
# Workspace 1 (Primary Workspace)
# ===================================
# Only BOT_TOKEN and APP_TOKEN are required
# TEAM_ID, TEAM_NAME, BOT_ID, BOT_USER_ID are auto-fetched from Slack API on startup

SLACK_WS1_BOT_TOKEN=xoxb-YOUR-BOT-TOKEN-HERE
SLACK_WS1_APP_TOKEN=xapp-YOUR-APP-TOKEN-HERE

# ===================================
# Workspace 2 (Optional - Add more workspaces as needed)
# ===================================

# SLACK_WS2_BOT_TOKEN=xoxb-...
# SLACK_WS2_APP_TOKEN=xapp-...

# ===================================
# Workspace 3 (Optional)
# ===================================

# SLACK_WS3_BOT_TOKEN=xoxb-...
# SLACK_WS3_APP_TOKEN=xapp-...

# ===================================
# Cloudflare R2 Storage
# ===================================

R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=notebooklm-media
R2_PUBLIC_URL=https://your-bucket.r2.cloudflarestorage.com

# ===================================
# NotebookLM Configuration
# ===================================

NOTEBOOKLM_EMAIL=your-email@gmail.com

# ===================================
# Optional: Development Settings
# ===================================

# Run Playwright in headless mode (true) or with visible browser (false)
PLAYWRIGHT_HEADLESS=false

# Directory for browser authentication data persistence
USER_DATA_DIR=./user-data

# ===================================
# Logging Configuration
# ===================================

# Log level: ERROR, WARN, INFO, DEBUG (default: INFO)
# - ERROR: Only critical errors
# - WARN: Warnings and errors
# - INFO: Informational messages (recommended for production)
# - DEBUG: All details including SQL queries (for troubleshooting)
LOG_LEVEL=INFO

# ===================================
# How to find these values:
# ===================================
#
# SLACK_SIGNING_SECRET:
#   - Go to https://api.slack.com/apps → Your App → Basic Information → App Credentials
#
# SLACK_WS*_BOT_TOKEN:
#   - Go to https://api.slack.com/apps → Your App → OAuth & Permissions
#   - Copy "Bot User OAuth Token" (starts with xoxb-)
#   - Install the app to each workspace you want to support
#
# SLACK_WS*_APP_TOKEN:
#   - Go to https://api.slack.com/apps → Your App → Basic Information → App-Level Tokens
#   - Generate token with connections:write scope (starts with xapp-)
#   - Note: APP_TOKEN is shared across all workspaces (use WS1's token for all)
#
# R2 Configuration:
#   - Go to Cloudflare Dashboard → R2 → Manage R2 API Tokens
#   - Account ID is shown at the top
#   - Create API token for Access Key ID and Secret Access Key
#   - Bucket name is your R2 bucket name
#   - Public URL is the custom domain or R2.dev URL for your bucket
#
# ===================================
# Note: Auto-fetched values
# ===================================
# The following are automatically retrieved from Slack API on startup:
#   - TEAM_ID (workspace identifier)
#   - TEAM_NAME (workspace name)
#   - BOT_ID (bot identifier)
#   - BOT_USER_ID (bot user identifier)
#
# This reduces environment variable management overhead while maintaining
# the same functionality. The bot will call Slack's auth.test API on startup.
